apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - <%= @slate::metallb_start_ip_range %>-<%= @slate::metallb_end_ip_range %>
